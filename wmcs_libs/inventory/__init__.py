"""Inventory and related classes and functions.

You should probably use the functions under `.libs`, unless you need dynamically loaded inventory data, then you can
use `.dynamic.get_inventory()` to load the inventory dynamically, and then inject it to the `.libs.*` functions.
"""

from __future__ import annotations
